plugins {
    id 'java-library'
}

configurations {
    libFatjar
    implementation.extendsFrom(libFatjar)
}

jar {
    from {
        configurations.libFatjar.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
